name: games_monorepo

packages:
  - packages/**
  - apps/**

command:
  bootstrap:
    usePubspecOverrides: true

scripts:
  analyze:
    run: melos exec -- flutter analyze
    description: Analyze all packages
  get:
    run: melos exec -- flutter pub get
    description: flutter pub get everywhere
